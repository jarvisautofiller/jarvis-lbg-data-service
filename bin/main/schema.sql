CREATE TABLE IF NOT EXISTS LBG_USER_DATA (
    PHONE_NUMBER VARCHAR(20) PRIMARY KEY,
    PROFESSION VARCHAR(100),
    ACCOUNT_NUMBER VARCHAR(100),
    IFSC_CODE VARCHAR(255),
    INCOME VARCHAR(20)
);

